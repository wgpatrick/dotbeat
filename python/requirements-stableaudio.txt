# dotbeat `beat source gen` backend — Stable Audio Open 1.0, run LOCALLY (Phase 39 Stream UB).
#
# This is the ONE licensing-clean, egress-free generative path for dotbeat's shareable-project
# thesis (research 103): you own the generated outputs, commercial use is permitted for individuals
# and orgs under $1M annual revenue at no fee (registration with Stability required for commercial
# use), and the license's distribution/attribution obligations attach to the MODEL/Materials, not
# to the generated audio files you commit into a project's media/ folder. See the license posture
# below and decisions.md D19.
#
# Installed and validated OWNER-SIDE only: torch + the ~couple-GB model weights are large and the
# HF/PyPI egress is blocked in dotbeat's dev/CI container, so `python/gen.py` degrades cleanly to an
# exit-3 "pip install -r python/requirements-stableaudio.txt" message here. The stdlib-only `stub`
# backend keeps CI green and proves the whole pipeline.
#
#   python3 -m venv python/.venv
#   python/.venv/bin/pip install -r python/requirements-stableaudio.txt
#   beat source gen --doctor        # confirm stableaudio reports ok:true
#
# Pins confirmed owner-side 2026-07-14 (PyPI reachable from the owner machine). stable-audio-tools
# 0.0.16..0.0.19 pin an old exact `pandas==2.0.2` that has no wheel for Python >=3.12 and fails to
# build from source (missing pkg_resources under modern setuptools) — 0.0.20 is the first release
# that both fixes this AND declares its real constraint: requires-python ">=3.10,<3.11". There is no
# version of stable-audio-tools installable on this machine's system Python (3.13); python/.venv
# MUST be built with a 3.10 interpreter (`brew install python@3.10`) for this backend to import.
torch>=2.0,<3
stable-audio-tools==0.0.20

# stable-audio-tools==0.0.20 itself pins PyWavelets==1.4.1, which predates numpy 2.0 and is
# ABI-incompatible with it ("numpy.dtype size changed" ImportError at runtime, confirmed
# owner-side 2026-07-14 — install order matters, this line must come AFTER stable-audio-tools so
# pip's resolver doesn't silently keep the broken pin). PyWavelets does not otherwise pin numpy, so
# raising it above 1.6 the moment this repo's numpy is 2.x is always safe.
PyWavelets>=1.6

# stable_audio_tools/models/lora/callbacks.py unconditionally `import pytorch_lightning` — reached
# at IMPORT time by get_pretrained_model()'s ordinary inference path (dit.py -> lora/__init__.py ->
# callbacks.py), not just LoRA training, even though pytorch_lightning is missing from
# stable-audio-tools' own declared dependencies (confirmed owner-side 2026-07-14: get_pretrained_model
# raises "No module named 'pytorch_lightning'" without this).
pytorch-lightning

# HF weights repo `stabilityai/stable-audio-open-1.0` confirmed owner-side 2026-07-14 via
# https://huggingface.co/api/models/stabilityai/stable-audio-open-1.0 (gated: "auto"). Accept the
# model license on that page and `huggingface-cli login` before the first real run.
#
# LICENSE — Stability AI Community License (https://stability.ai/community-license-agreement):
#   * Research + non-commercial use: free.
#   * Commercial use: free for individuals/orgs under $1M annual revenue, provided you REGISTER for
#     a Community License with Stability; the license terminates above $1M (Enterprise license
#     required there).
#   * You OWN the generated outputs. The distribution/attribution terms (ship a copy of the license,
#     display "Powered by Stability AI") attach to redistributing the MODEL/Materials/derivatives,
#     NOT to the individual output .wav files — so committing generated one-shots into a public
#     .beat project's media/ folder is clean. dotbeat carries the "Powered by Stability AI"
#     attribution in its docs (python/README.md) as the tool-integration obligation.
