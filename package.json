{
  "name": "dotbeat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Core, daemon, and CLI for a git-native, agent-native DAW.",
  "bin": {
    "beat": "./cli/beat.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/test/fixtures dist/presets && cp test/fixtures/*.json dist/test/fixtures/ && cp presets/*.json dist/presets/",
    "test": "npm run build && node --test dist/test/*.test.js",
    "beat": "node cli/beat.mjs",
    "render": "node cli/render.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "node-web-audio-api": "file:../upstream/node-web-audio-api",
    "playwright-core": "^1.61.1",
    "tone": "^15.1.22",
    "typescript": "~5.6.2"
  },
  "license": "MIT"
}
