{
  "name": "beatlab-daw",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Planning + Phase-0 core for a git-native, agent-native DAW.",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/test/fixtures && cp test/fixtures/*.json dist/test/fixtures/",
    "test": "npm run build && node --test dist/test/*.test.js",
    "render": "node cli/render.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "playwright-core": "^1.61.1",
    "typescript": "~5.6.2"
  }
}
